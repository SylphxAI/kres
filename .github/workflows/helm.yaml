# THIS FILE WAS AUTOMATICALLY GENERATED BY KRES, PLEASE DO NOT EDIT.
#
# Generated on 2026-08-05T06:47:23Z by kres undefined.

concurrency:
  group: helm-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
'on':
  push:
    tags:
      - v*
  pull_request:
    branches:
      - main
      - release-*
    paths:
      - test/**
  merge_group:
    types:
      - checks_requested
name: helm
jobs:
  default:
    permissions:
      actions: read
      contents: write
      id-token: write
      issues: read
      packages: write
      pull-requests: read
    runs-on:
      - self-hosted
      - sylphx-linux-standard
    steps:
      - name: gather-system-info
        id: system-info
        uses: kenchan0130/actions-system-info@59699597e84e80085a750998045983daa49274c4 # version: v1.4.0
        continue-on-error: true
      - name: print-system-info
        run: |
          MEMORY_GB=$((${{ steps.system-info.outputs.totalmem }}/1024/1024/1024))

          OUTPUTS=(
            "CPU Core: ${{ steps.system-info.outputs.cpu-core }}"
            "CPU Model: ${{ steps.system-info.outputs.cpu-model }}"
            "Hostname: ${{ steps.system-info.outputs.hostname }}"
            "NodeName: ${NODE_NAME}"
            "Kernel release: ${{ steps.system-info.outputs.kernel-release }}"
            "Kernel version: ${{ steps.system-info.outputs.kernel-version }}"
            "Name: ${{ steps.system-info.outputs.name }}"
            "Platform: ${{ steps.system-info.outputs.platform }}"
            "Release: ${{ steps.system-info.outputs.release }}"
            "Total memory: ${MEMORY_GB} GB"
          )

          for OUTPUT in "${OUTPUTS[@]}";do
            echo "${OUTPUT}"
          done
        continue-on-error: true
      - name: checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # version: v7.0.0
      - name: Unshallow
        run: |
          git fetch --prune --unshallow
      - name: Configure Docker Buildx environment
        run: |
          mkdir -p "$RUNNER_TEMP/kres-buildx"
          printf '%s\n' "BUILDX_CONFIG=$RUNNER_TEMP/kres-buildx" >> "$GITHUB_ENV"
      - name: Set up Docker Buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # version: v4.2.0
        with:
          driver: docker-container
        timeout-minutes: 10
      - name: Set up Helm
        uses: Azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # version: v5.0.1
        with:
          version: v3.21.3
      - name: Configure Helm environment
        run: |
          mkdir -p "$RUNNER_TEMP/kres-helm/config" "$RUNNER_TEMP/kres-helm/cache" "$RUNNER_TEMP/kres-helm/data"
          printf '%s\n' \
            "HELM_CONFIG_HOME=$RUNNER_TEMP/kres-helm/config" \
            "HELM_CACHE_HOME=$RUNNER_TEMP/kres-helm/cache" \
            "HELM_DATA_HOME=$RUNNER_TEMP/kres-helm/data" >> "$GITHUB_ENV"
      - name: Login to registry
        if: github.event_name != 'pull_request' && github.event_name != 'merge_group'
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # version: v4.4.0
        with:
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io
          username: ${{ github.repository_owner }}
      - name: Lint chart
        if: (github.event_name == 'pull_request' || github.event_name == 'merge_group')
        run: |
          helm lint test/test-helm-chart
      - name: Template chart
        if: (github.event_name == 'pull_request' || github.event_name == 'merge_group')
        run: |
          helm template -f test/test-helm-chart/ci-values.yaml test-helm-chart test/test-helm-chart
      - name: Install unit test plugin
        if: (github.event_name == 'pull_request' || github.event_name == 'merge_group')
        run: |
          make helm-plugin-install
      - name: Unit test chart
        if: (github.event_name == 'pull_request' || github.event_name == 'merge_group')
        run: |
          make chart-unittest
      - name: Generate schema
        if: (github.event_name == 'pull_request' || github.event_name == 'merge_group')
        run: |
          make chart-gen-schema
      - name: Generate docs
        if: (github.event_name == 'pull_request' || github.event_name == 'merge_group')
        run: |
          make helm-docs
      - name: helm login
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          helm registry login -u ${{ github.repository_owner }} -p ${{ secrets.GITHUB_TOKEN }} ghcr.io
      - name: Release chart
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          make helm-release
